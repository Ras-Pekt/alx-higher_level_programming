#!/usr/bin/python3
def uppercase(str):
    for ch in str:
        if ord(ch) >= 97 and ord(ch) <= 122:
            CH = ord(ch) + 32
            print("{}".format(chr(CH)), end="")

print("\n", end="")
